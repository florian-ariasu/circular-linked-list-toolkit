-- Tema 1 SDA --
Ariasu Florian-Andrei - 325CB

Aceasta tema a fost implementata astfel:

main.c - Am deschis cele 2 fisiere, am initializat un tren si o coada
iar apoi am apelat functia ReadCommand de la care porneste rezolvarea
task-urilor. La final am dat free la tren si am distrus coada. In final 
am inchis fisierele de input si output.

train.c - Aici sunt toate functiile din cerinta implementate.
Am functia de initializare a trenului, o functie care aloca celule noi, 
si toate operatiile: insert_left, insert_right, move_left, move_right,
write, show_current si show care se executa direct, clear_cell si clear_all, 
iar la final cele 3 functii de search pentru care am tratat cazurile
in care cuvintele aveau lungimea 2 sau 3 separat in afara buclei while.
Clear_all readuce trenul in starea initiala adica santinela plus vagonul 1 
in care am si mecanicul, iar clear_cell sterge celula in care se afla mecanicul 
si reface legaturile aferente.

utils.c - Aici am functia de baza a programului care citeste
comenzile din fisier si le introduce in coada plus o functie care ma ajuta
sa extrag cuvantul de cautat la cele 3 operatii search.

queue.c - Aici am operatiile de baza ale cozii, plus operatia de switch, 
pentru care am folosit o stiva si o coada astfel incat folosindu ma
de push pop intrq si extrq sa pot face reverse la elementele cozii. Tot 
aici am adaugat si comanda execute care apeleaza functiile din coada.

stack.c - Aici am functiile necesare pentru a crea si a manipula o stiva, 
pe care le folosesc ulterior doar pentru operatia switch.

Fisierele header sunt: train.h, utils.h, queue.h, stack.h in care am structurile 
necesare implementarii complete a programului si definitiile functiilor folosite 
in main.c si celelalte fisiere .c.

Mentionez ca nu am facut bonusul cu valgrind.
